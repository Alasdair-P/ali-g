- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 100.0, epochs: 200, loss: map, tag: AP3, max_norm: 50}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 100.0, epochs: 200, loss: map, tag: AP3, max_norm: 100}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 100.0, epochs: 200, loss: map, tag: AP3, max_norm: 150}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 100.0, epochs: 200, loss: map, tag: AP3, max_norm: 200}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 100.0, epochs: 200, loss: map, tag: AP3, max_norm: 250}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 10.0, epochs: 200, loss: map, tag: AP3, max_norm: 50}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 10.0, epochs: 200, loss: map, tag: AP3, max_norm: 100}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 10.0, epochs: 200, loss: map, tag: AP3, max_norm: 150}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 10.0, epochs: 200, loss: map, tag: AP3, max_norm: 200}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 10.0, epochs: 200, loss: map, tag: AP3, max_norm: 250}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 1.0, epochs: 200, loss: map, tag: AP3, max_norm: 50}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 1.0, epochs: 200, loss: map, tag: AP3, max_norm: 100}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 1.0, epochs: 200, loss: map, tag: AP3, max_norm: 150}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 1.0, epochs: 200, loss: map, tag: AP3, max_norm: 200}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 1.0, epochs: 200, loss: map, tag: AP3, max_norm: 250}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 0.1, epochs: 200, loss: map, tag: AP3, max_norm: 50}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 0.1, epochs: 200, loss: map, tag: AP3, max_norm: 100}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 0.1, epochs: 200, loss: map, tag: AP3, max_norm: 150}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 0.1, epochs: 200, loss: map, tag: AP3, max_norm: 200}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 3, eta: 0.1, epochs: 200, loss: map, tag: AP3, max_norm: 250}
