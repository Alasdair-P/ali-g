#- {model: wrn, dataset: cifar10, batch_size: 500, opt: alig, eta: 0.1, epochs: 200, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd-sb, k: 5, eta: 0.1, epochs: 50, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: alig, eta: 0.01, epochs: 200, loss: map, tag: test, val_size: 5000}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd-sb, k: 5, eta: 0.01, epochs: 50, loss: map, tag: test, val_size: 5000}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: alig, eta: 1.0, epochs: 200, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 5, eta: 1.0, epochs: 200, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 5, eta: 0.1, epochs: 200, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 5, eta: 0.01, epochs: 200, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 5, eta: 10.0, epochs: 200, loss: map, tag: test, val_size: 2500}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 5, eta: 1.0, epochs: 200, loss: map, tag: 1}
#- {model: wrn, dataset: cifar10, batch_size: 250, opt: alig, eta: 1.0, epochs: 200, loss: map, tag: 1}
#- {model: wrn, dataset: cifar10, batch_size: 250, opt: sbd, k: 5, eta: 1.0, epochs: 200, loss: map, tag: 1}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: alig, eta: 100.0, epochs: 200, loss: map, tag: test, val_size: 2500}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: alig, eta: 1.0, epochs: 200, loss: map, tag: AP, max_norm: 100}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sbd, k: 5, eta: 1.0, epochs: 200, loss: map, tag: AP, max_norm: 100}
- {model: wrn, dataset: cifar10, batch_size: 250, opt: alig, eta: 1.0, epochs: 200, loss: ndcg, tag: ndcg}
#- {model: wrn, dataset: cifar10, batch_size: 120, opt: alig, eta: 1.0, epochs: 200, loss: map, tag: 1}
#- {model: wrn, dataset: cifar10, batch_size: 120, opt: sbd, k: 5, eta: 1.0, epochs: 200, loss: map, tag: 1}
