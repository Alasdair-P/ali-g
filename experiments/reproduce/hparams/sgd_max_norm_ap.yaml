#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.01, max_norm: 50, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.1, max_norm: 50, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 1.0, max_norm: 50, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 10.0, max_norm: 50, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.01, max_norm: 100, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.1, max_norm: 100, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 1.0, max_norm: 100, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 10.0, max_norm: 100, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.01, max_norm: 150, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.1, max_norm: 150, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 1.0, max_norm: 150, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 10.0, max_norm: 150, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.01, max_norm: 200, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.1, max_norm: 200, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 1.0, max_norm: 200, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 10.0, max_norm: 200, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.01, max_norm: 250, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 0.1, max_norm: 250, loss: map, tag: map500, epochs: 200}
#- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 1.0, max_norm: 250, loss: map, tag: map500, epochs: 200}
- {model: wrn, dataset: cifar10, batch_size: 500, opt: sgd, k: 1, eta: 10.0, max_norm: 250, loss: map, tag: map500, epochs: 200}
